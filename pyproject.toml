[tool.ruff]
line-length = 140
target-version = "py311"
exclude = ["gerar_roteiro_pdf.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
ignore_missing_imports = true
disallow_untyped_decorators = false
files = ["modulos", "core", "components", "views", "app.py", "tests"]
exclude = ["gerar_roteiro_pdf.py"]

[[tool.mypy.overrides]]
module = ["streamlit.*", "plotly.*", "reportlab.*", "pandas.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
